import { DashboardLayout } from "src/components";
import { BookingList } from "../components/";

export const Bookings = () => {
  return (
    <DashboardLayout>
      <BookingList />
    </DashboardLayout>
  );
};
